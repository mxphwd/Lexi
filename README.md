# Lexi

Lexi is Alphaine’s deterministic, zero-generative language model prototype, built from explicit linguistic rules, typed knowledge, and inspectable mechanical reasoning rather than token prediction. It answers from traceable evidence and abstains when it cannot support a response.
