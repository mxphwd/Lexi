# DV14 evaluation boundary

`development.jsonl` contains disclosed structural regressions derived from the DV14 audit. It may guide implementation and therefore cannot measure public-use accuracy.

`independent.jsonl` and `calibration.jsonl` are intentionally empty until independently authored, human-reviewed cases are supplied. Runtime code must never import this directory. The RC gate remains closed while either cohort is undersized.
