# Proposition Realizer

The realizer converts typed graph answers into reviewed English structures. It
handles open, boolean, count, and comparison answers and preserves recorded
qualifiers. Style changes affect presentation only; they cannot add facts.

Each new graph predicate should receive a dedicated realization when the generic
property sentence would be unclear or ungrammatical.
